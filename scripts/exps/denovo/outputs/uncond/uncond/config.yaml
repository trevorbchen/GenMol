name: uncond
sampler:
  _target_: genmol.sampler.Sampler
reward:
  type: none
  alpha: 0.0
  target: null
  params: {}
model_path: ../../../model.ckpt
output_dir: outputs
num_samples: 100
softmax_temp: 0.8
randomness: 0.5
min_add_len: 40
seed: 0
